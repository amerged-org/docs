---
title: "Forbidden"
description: "What the `forbidden` error means and what to do next."
---

The API, CLI and MCP return `forbidden` with HTTP 403. Retrying the same request unchanged does not help.

## What to do

Check organization access.

Keep the `request_id` from the error when you [report the problem](/feedback).
