---
title: "Confirmation invalid"
description: "What the `confirmation_invalid` error means and what to do next."
---

The API, CLI and MCP return `confirmation_invalid` with HTTP 409. Retrying the same request unchanged does not help.

## What to do

Use the confirmation token from a fresh matching plan.

Keep the `request_id` from the error when you [report the problem](/feedback).
