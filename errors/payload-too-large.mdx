---
title: "Payload too large"
description: "What the `payload_too_large` error means and what to do next."
---

The API, CLI and MCP return `payload_too_large` with HTTP 413. Retrying the same request unchanged does not help.

## What to do

Reduce the request size.

Keep the `request_id` from the error when you [report the problem](/feedback).
