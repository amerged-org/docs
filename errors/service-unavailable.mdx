---
title: "Service unavailable"
description: "What the `service_unavailable` error means and what to do next."
---

The API, CLI and MCP return `service_unavailable` with HTTP 503. Retrying the same request with backoff can succeed.

## What to do

Retry later.

Keep the `request_id` from the error when you [report the problem](/feedback).
