---
title: "Invalid request"
description: "What the `invalid_request` error means and what to do next."
---

The API, CLI and MCP return `invalid_request` with HTTP 400. Retrying the same request unchanged does not help.

## What to do

Check the request and retry.

Keep the `request_id` from the error when you [report the problem](/feedback).
